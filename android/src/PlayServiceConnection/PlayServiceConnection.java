package PlayServiceConnection;

/**
 * Created by João on 27/04/2017.
 */

public class PlayServiceConnection {
    private void startQuickGame() {
        /*
        // auto-match criteria to invite one random automatch opponent.
        // You can also specify more opponents (up to 3).
        Bundle am = RoomConfig.createAutoMatchCriteria(1, 1, 0);

        // build the room config:
        RoomConfig.Builder roomConfigBuilder = makeBasicRoomConfigBuilder(); //method to create roomconfiguration
        roomConfigBuilder.setAutoMatchCriteria(am);
        RoomConfig roomConfig = roomConfigBuilder.build();

        // create room:
        Games.RealTimeMultiplayer.create(mGoogleApiClient, roomConfig); //on mGoogleApiClient is de googleapiclient when user connect to the google play services!


        // go to game screen
        */
    }
}
